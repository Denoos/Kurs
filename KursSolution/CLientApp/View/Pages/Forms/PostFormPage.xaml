<Page x:Class="CLientApp.View.Pages.Forms.PostFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          mc:Ignorable="d"
    Title="Должности"
    Background="White">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Border BorderThickness="3" Grid.Column="1" BorderBrush="{Binding Settings.Color}" HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="30" Padding="20" Background="#FFF5F5F5">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
                <TextBlock Text="Должности" FontSize="{Binding Settings.FontSize}" FontWeight="Bold" Foreground="{Binding Settings.Color}" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Название" FontSize="{Binding Settings.FontSize}" FontWeight="Bold" Foreground="{Binding Settings.Color}" 
                           HorizontalAlignment="Left" Margin="0,0,0,20"/>
                <TextBox Name="UsernameTextBox" 
                         Text="{Binding Item.Title}"
                         Margin="0,0,0,10" 
                         Height="40" 
                         FontSize="16" 
                         Padding="5"/>


                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button IsEnabled="{Binding IsEnabled}" Content="Сохранить" 
                            FontSize="{Binding Settings.FontSize}"
                            Click="Save_Click"
                            Width="200" 
                            Height="40" 
                            Background="{Binding Settings.Color}" 
                            Foreground="White" 
                            FontWeight="Bold" 
                            />

                    <Button Content="Назад" 
                            Width="230"
                            FontSize="{Binding Settings.FontSize}"
                            Click="Exit_Click"
                            Height="40" 
                            Background="{Binding Settings.Color}" 
                            Foreground="White" 
                            FontWeight="Bold" 
                            Margin="10,0,0,0" 
                            />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>