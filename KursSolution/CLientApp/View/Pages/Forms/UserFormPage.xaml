<Page x:Class="CLientApp.View.Pages.Forms.UserFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CLientApp.View.Pages.Forms"
          mc:Ignorable="d"
    Title="Пользователь"
    Background="White">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Border BorderThickness="3" Grid.Column="1" BorderBrush="#FF4500" HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="30" Padding="20" Background="#FFF5F5F5">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
                <TextBlock Text="Пользователь" FontSize="24" FontWeight="Bold" Foreground="#FF4500" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Логин" FontSize="18" FontWeight="Bold" Foreground="#FF4500" 
                           HorizontalAlignment="Left" Margin="0,0,0,20"/>
                <TextBox IsEnabled="{Binding IsEnabled}" Name="UsernameTextBox" 
                         Text="{Binding Item.Login}"
                         Margin="0,0,0,10" 
                         Height="40" 
                         FontSize="16" 
                         Padding="5"/>
                <TextBlock Text="Пароль" FontSize="18" FontWeight="Bold" Foreground="#FF4500" 
                           HorizontalAlignment="Left" Margin="0,0,0,20"/>
                <TextBox IsEnabled="{Binding IsEnabled}"
                         Text="{Binding Item.Password}"
                         Margin="0,0,0,10" 
                         Height="40" 
                         FontSize="16" 
                         Padding="5"/>
                <TextBlock Text="Роль" FontSize="18" FontWeight="Bold" Foreground="#FF4500" 
                           HorizontalAlignment="Left" Margin="0,0,0,20"/>
                <TextBox IsEnabled="False"
                         Text="{Binding Item.IdRoleNavigation.Ttle}"
                         Margin="0,0,0,10" 
                         Height="40" 
                         FontSize="16" 
                         Padding="5"/>

                <ComboBox DisplayMemberPath="Title"  IsEnabled="{Binding IsEnabled}" ItemsSource="{Binding Items}" Margin="20, 10" Height="40" SelectedItem="{Binding Item.IdRoleNavigation, Mode=TwoWay}" />


                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button IsEnabled="{Binding IsEnabled}" Content="Сохранить" 
                            FontSize="18"
                            Click="Save_Click"
                            Width="200" 
                            Height="40" 
                            Background="#FF4500" 
                            Foreground="White" 
                            FontWeight="Bold" 
                            />

                    <Button Content="Назад" 
                            Width="230"
                            FontSize="18"
                            Click="Exit_Click"
                            Height="40" 
                            Background="#FF4500" 
                            Foreground="White" 
                            FontWeight="Bold" 
                            Margin="10,0,0,0" 
                            />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>